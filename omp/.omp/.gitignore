# Deny by default, including auth, DB sidecars, sessions, blobs, plugins,
# caches, logs, profiles, installation identity and future upstream state.
/*
!/.gitignore
!/agent/
/agent/*
!/agent/config.yml
!/agent/models.yml
