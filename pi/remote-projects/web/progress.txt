## 2026-09-08 - Bounded source-only PiWeb browser support
- Added readable pinned source patch (+266/-81 lines across 29 native files), exact apply checks, canonical helper copy/checksum, isolated build/pack and focused tests. All successful writes stayed under this owned web directory; sibling helper was read/copied only.
- Reason: reuse native PiWeb/SDK/extension for browser remote chat/tools and !/!! while visibly disabling/rejecting still-local panels for bound Remote/unknown sessions.
- Verified: source checksum and report npm SRIs; clean apply/wrong-version/reapply rejection; 14 focused real SDK/HTTP/React tests; 950 native regression tests; focused ESLint; complete production build/pack; scratch npm 0.9.0 replacement and installed-package loopback Basic/Host/Origin/route checks.
- Build issues resolved: older Python lacks tarfile filter keyword (explicit member validation used); npm disallows user/global config both pointing to /dev/null (distinct empty isolated config paths used); ignored install scripts require explicit node-pty rebuild; Jiti JSX cache needed fsCache:false in focused harness; Node fetch normalizes Host (raw node:http used to test Host protection).
- External command wrapper timed out during build tracing at 120 seconds, and during the combined cold pipeline at 300 seconds. Separate native build completed successfully with longer allowance; then npm pack and replacement smoke passed. Unchanged export-route dynamic-import webpack warning remains.
- Runtime/installer/shared config/vault/Git publication untouched. Real SSH/model requests and hydrated browser E2E not performed. Team remains held by reprovision. Installer must later pin the artifact and matching managed extension/helper inputs.
- Required absolute /progress.txt is absent and not writable (PermissionDenied). No shared /workspace/progress.txt was modified; this owned status file records the changelog instead.
